const JSX = (
    <div>
        <h2>Welcome to React!</h2>
        <br /> {/* All element must be closed in JSX AND can be closed like this - if they have no content */}
        <p>Be sure to close all tags!</p>
        <hr />
    </div>
);